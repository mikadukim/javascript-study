let x = [1, 2, 3];

const test_func = (leng) => {
    if (x.length >= leng) {
        console.log(x[1])
    } else {
        console.log('hogege')
    }
};

const unko = {
    color: 'pink',
    size: 'large',
    purfume: 'mint'
};

document.getElementsByTagName('button')[0].addEventListener('click', () => {
    window.alert('Hogefw!!!')
});
